# Отчет по задаче №8: Фронтенд списка заданий

## Что сделано

### 1. Веб-обработчик (Handler)
- Создал `routers/web/edu/assignments.go`.
- В нем реализовал обработчик `Assignments`, который:
    - Инициализирует репозиторий через `edu.NewRepository`.
    - Использует вспомогательную функцию `getSQLRunner` для адаптации движка xorm (Forgejo) под наш интерфейс `SQLRunner`.
    - Достает список заданий через `svc.GetAssignments(ctx, 0)`.
    - Рендерит шаблон `edu/assignment_list`.

### 2. Шаблон (Template)
- Создал `templates/edu/assignment_list.tmpl`.
- Подключил базовые хедеры и футеры (`base/head.tmpl`).
- Сделал вывод списка заданий в виде таблицы: название, описание и дедлайн.

### 3. Регистрация маршрутов
- Внес изменения в `routers/web/web.go`.
- Добавил маршрут `/edu/assignments`, доступ к которому есть только у авторизованных пользователей (`reqSignIn`).

## Результат
Страница списка заданий готова к работе. Дальше по плану — личный кабинет студента.
