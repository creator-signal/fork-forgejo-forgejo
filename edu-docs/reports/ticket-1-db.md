# Отчет по задаче №1: Проектирование схемы БД

## Что сделано
1. **SQL Миграции**:
   - Создана директория `db/migrations`.
   - Написана миграция `20260117150000_create_edu_tables.up.sql` для создания таблиц:
     - `edu_assignments` (Задания)
     - `edu_submissions` (Решения студентов)
     - `edu_test_results` (Результаты автотестов)
   - Написана обратная миграция (`down.sql`).

2. **Go Модели**:
   - Создан пакет `internal/edu`.
   - В файле `internal/edu/models.go` описаны структуры `Assignment`, `Submission`, `TestResult`.

## Как верифицировать
1. Проверить наличие SQL файлов в `db/migrations`.
2. Убедиться, что структуры Go соответствуют полям в БД (типы данных, JSON-теги).
