# Отчет по задаче №11: Юнит-тестирование

## Что сделано

### 1. Охват тестами
- Пакет `internal/edu`:
    - `TestCreateAssignment`: проверил создание заданий и валидацию данных.
    - `TestGetAssignments`: проверил получение списка.
    - `TestGetSubmissions`: проверил логику выгрузки решений.
    - `TestJoinAssignment`: протестировал кейсы "задание уже взято" и "новое задание".

### 2. Моки
- Доработал `MockRepository` и `MockRepoForker` в `mock_test.go`, чтобы они поддерживали все новые методы сервиса.

## Результат
Запустил тесты командой `go test -v ./internal/edu/...`. Все тесты пройдены успешно (PASS). Бизнес-логика сервиса работает как надо.

Дальше по плану интеграционные тесты на уровне API.
