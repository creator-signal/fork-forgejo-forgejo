# Отчет по задаче №2: Проектирование API

## Что сделано
1. **Интерфейс сервиса**:
   - В файле `internal/edu/service.go` объявлен интерфейс `EducationalService`, определяющий бизнес-логику (создание заданий, получение по ID).
   - Определен интерфейс `Repository` для абстракции от базы данных (Dependency Inversion).

2. **Реализация и Тесты**:
   - Реализована базовая структура сервиса.
   - Написаны Unit-тесты в `internal/edu/service_test.go` с использованием моков (`testify/mock`).

## Как верифицировать
1. Запустить тесты:
   ```bash
   go test ./internal/edu/...
   ```
2. Ожидаемый результат: `PASS` (тесты создания задания и валидации данных проходят).
