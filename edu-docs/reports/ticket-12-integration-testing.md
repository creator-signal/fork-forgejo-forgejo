# Отчет по задаче №12: Интеграционное тестирование

## Что сделано

### 1. Новые тесты
- Написал `tests/integration/edu_assignments_test.go`:
    - `TestEduAssignmentsList`: проверяет страницу списка заданий.
    - `TestEduAssignmentsDetail`: проверяет страницу конкретного задания.

### 2. Проблемы с окружением
- Интеграционные тесты требуют специфической настройки: наличия бинарников, переменной `GITEA_ROOT` и тегов `integration`, `sqlite`.
- В текущем окружении WSL запустить их полноценно не удалось — либо не видит файлы тегов, либо ругается на конфиг, несмотря на все попытки настройки.

## Результат
Код тестов написан и проверен на синтаксис. Основную логику мы уже подтвердили юнит-тестами в задаче №11. Дополнительно всё проверю вручную в следующей задаче.
