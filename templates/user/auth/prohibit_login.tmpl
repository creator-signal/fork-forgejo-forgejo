{{template "base/head" .}}
<div class="page-content user activate">
	<main id="main" aria-label="{{.Title}}" class="ui middle very relaxed page grid">
		<div class="column">
			<form class="ui form tw-max-w-2xl tw-m-auto">
				<h2 class="ui top attached header">
					{{ctx.Locale.Tr "auth.prohibit_login"}}
				</h2>
				<div class="ui attached segment">
					<p>{{ctx.Locale.Tr "auth.prohibit_login_desc"}}</p>
				</div>
			</form>
		</div>
	</main>
</div>
{{template "base/footer" .}}
