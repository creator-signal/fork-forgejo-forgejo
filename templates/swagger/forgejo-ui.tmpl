<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>Forgejo API</title>
		<link href="{{AssetURI "css/swagger.css"}}" rel="stylesheet">
	</head>
	<body>
		<a class="swagger-back-link" href="{{AppUrl}}">{{svg "octicon-reply"}}{{.locale.Tr "return_to_forgejo"}}</a>
		<div id="swagger-ui" data-source="{{AssetUrlPrefix}}/forgejo/api.{{.APIVersion}}.yml"></div>
		<script src="{{AssetURI "js/forgejoswagger.js"}}"></script>
	</body>
</html>
