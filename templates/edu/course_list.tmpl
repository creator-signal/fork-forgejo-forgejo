{{template "base/head" .}}
<div role="main" aria-label="{{.Title}}" class="page-content edu courses">
	<div class="ui container">
		<div class="ui grid">
			<div class="column">
				<div class="ui secondary menu">
					<div class="header item">
						<h1 class="ui header" style="margin:0">
							{{svg "octicon-mortar-board" 32}}
							{{ctx.Locale.Tr "edu.courses"}}
						</h1>
					</div>
					<div class="right menu">
						<a class="ui positive button" href="{{AppSubUrl}}/edu/teacher/courses/new">
							{{svg "octicon-plus"}}
							{{ctx.Locale.Tr "edu.new_course"}}
						</a>
					</div>
				</div>

				<div class="ui secondary pointing menu">
					<a class="item" href="{{AppSubUrl}}/edu/teacher/assignments">{{ctx.Locale.Tr "edu.assignments"}}</a>
					<a class="active item" href="{{AppSubUrl}}/edu/teacher/courses">{{ctx.Locale.Tr "edu.courses"}}</a>
				</div>
			</div>
		</div>

		<div class="ui divider"></div>

		{{if .Courses}}
			<div class="ui three stackable cards">
				{{range .Courses}}
					<div class="ui fluid card">
						<div class="content">
							<div class="header">
								<a href="{{AppSubUrl}}/edu/teacher/courses/{{.ID}}">{{.Name}}</a>
							</div>
							<div class="meta">
								{{if .StartUnix}}
									<span class="date">{{svg "octicon-calendar"}} {{.StartDate.Format "02 Jan 2006"}}</span>
								{{end}}
								{{if .EndUnix}}
									<span class="date"> — {{.EndDate.Format "02 Jan 2006"}}</span>
								{{end}}
							</div>
							<div class="description">
								{{.Description | SanitizeHTML}}
							</div>
						</div>
						<div class="extra content">
							{{if .IsActive}}
								<span class="ui green label">{{ctx.Locale.Tr "edu.active"}}</span>
							{{else}}
								<span class="ui red label">{{ctx.Locale.Tr "edu.ended"}}</span>
							{{end}}
							<a class="ui primary button right floated" href="{{AppSubUrl}}/edu/teacher/courses/{{.ID}}">
								{{svg "octicon-eye"}} {{ctx.Locale.Tr "edu.view"}}
							</a>
						</div>
					</div>
				{{end}}
			</div>
		{{else}}
			<div class="ui placeholder segment">
				<div class="ui icon header">
					{{svg "octicon-repo"}}
					{{ctx.Locale.Tr "edu.no_courses"}}
				</div>
				<a class="ui primary button" href="{{AppSubUrl}}/edu/teacher/courses/new">{{ctx.Locale.Tr "edu.create_first_course"}}</a>
			</div>
		{{end}}
	</div>
</div>
<style>
	.edu.courses .card {
		box-shadow: 0 4px 6px rgba(0,0,0,0.1);
		transition: transform 0.2s;
	}
	.edu.courses .card:hover {
		transform: translateY(-5px);
	}
</style>
{{template "base/footer" .}}
