Mirrors are now automatically disabled after a configurable number of consecutive failures occur.
This option can be changed in the configuration (`MAX_FAILED_SYNC_COUNT` under the `[mirror]` section), and defaults to `10`.
You can set this number to `0` if you never want it to be disabled automatically (the old behavior).
