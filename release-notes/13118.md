Added a security audit log. When enabled (`[audit]` `ENABLED = true`), Forgejo records security-relevant events — successful sign-ins, personal access token and OAuth2 application creation, enabling/disabling two-factor authentication, password changes and email address changes — and a site administrator can review them under **Site Administration → Audit log**.
