Bash/ZSH completions in `./contrib/completions` were regenerated from current source.
However, they are now marked as **deprecated** and should be generated on the fly using `forgejo completion <shell>` instead.
