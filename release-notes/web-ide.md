feat: add a browser-based VS Code Web IDE. When `[repository.webide] ENABLED = true`, repositories can turn on the "Web IDE" unit to open files in a client-side VS Code editor (browse, edit and multi-file commit) on the main Forgejo origin. It runs entirely in the browser and commits through the existing repository services, so it adds no per-user server process. Disabled by default.
