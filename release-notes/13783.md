Pushing a container image no longer fails with `500 Internal Server Error` on blob requests when the client cancels part of the upload. Docker opens many blob HEAD requests in parallel and drops the ones it no longer needs, and those canceled requests were reported as server errors, which could abort the whole push and flood the log with misleading error entries. A canceled or timed out request is now answered with `499 Client Closed Request` instead.
