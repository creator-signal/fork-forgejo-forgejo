Add a new redirect from `/new/` to `/repo/create`. This copies the behaviour of https://github.com/new for
Forgejo users.

If a Forgejo instance has users or organizations named `new`, they will also need to be renamed
before the upgrade, as this is now a reserved name.
