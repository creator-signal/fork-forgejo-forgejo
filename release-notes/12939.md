Fixes markdown rendering in repositories for absolute links (those starting with `/`) in rendered content (comments, issues, markdown, orgmode), they are now resolved relative to the repository root. Content nested in a directory is no longer resolved relative to that directory unless you use `./` instead of `/`.