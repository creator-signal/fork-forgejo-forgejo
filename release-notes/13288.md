Repositories can now be migrated from Bitbucket Data Center (Bitbucket Cloud uses a different API and is not covered), including pull requests with their reviews and comments (Bitbucket Data Center has no issues, wiki, milestones or releases). Authentication uses an HTTP access token (personal, project or repository access token), and the Bitbucket instance must have HTTP(S) SCM hosting enabled for the git clone to succeed.
