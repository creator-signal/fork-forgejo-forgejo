Add generic "[pfx]KEYS_ACCEPTED = <alg>:[<base64>|<uri>] ..." configuration to configure accepted keys for JWT validation and activate it for [oauth2] as JWT_KEYS_ACCEPTED. Require exp claim by default for JWTs.
